import 'package:flame/game.dart';

/// Flame game shell — ball, goal, and UI will render on a transparent layer
/// composited above the camera preview.
class VisionFootballGame extends FlameGame {
  // TODO: Add game components (ball, goal, score UI) in a later step.
}
